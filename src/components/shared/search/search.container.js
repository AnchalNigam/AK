import React from 'react';
import {SearchBox} from './search.presentation';

class SearchContainer extends React.Component {
    render() {
        return (
           <SearchBox/> 
        );
      }
}

export default SearchContainer;